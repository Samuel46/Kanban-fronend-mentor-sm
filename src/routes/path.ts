function path(root: string, sublink: string) {
	return `${root}${sublink}`;
}
const ROOT_KANBAN = "/kanban";
const ROOT = "/";

// -------------------------------------------------------------

export const PATH_KANBAN = {
	root: ROOT,
	kanban: ROOT_KANBAN,
};
